mutation AddToEvent(
  $productVariantId: Int!
  $userEventId: Int!
  $quantity: Float!
) {
  addToEvent(
    productVariantId: $productVariantId
    userEventId: $userEventId
    quantity: $quantity
  ) {
    productVariantId
    userEventId
    quantity
  }
}
