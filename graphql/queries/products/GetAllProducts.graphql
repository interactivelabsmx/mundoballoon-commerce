query GetAllProducts($first: Int = 5, $after: String) {
  allProducts(first: $first, after: $after, order: [{ price: ASC }]) {
    nodes {
      ...ProductDetails
    }
    pageInfo {
      hasNextPage
      endCursor
      hasPreviousPage
      startCursor
    }
  }
}
