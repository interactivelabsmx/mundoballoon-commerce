query GetProductQuickView($productId: Int!) {
  productQuickView(productId: $productId) {
    product {
      ...ProductQuickView
    }
    variants {
      ...VariantDisplay
    }
    variantValues {
      ...VariantValueDisplay
    }
  }
}
