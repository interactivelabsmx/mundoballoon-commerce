mutation AddToCart(
  $sku: String!
  $quantity: Decimal!
  $price: Decimal!
  $productVariantId: Int!
) {
  addToCart(
    sku: $sku
    quantity: $quantity
    price: $price
    productVariantId: $productVariantId
  ) {
    sku
    quantity
    price
    productVariantId
  }
}
