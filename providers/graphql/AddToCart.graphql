mutation AddToCart(
  $sku: String!
  $quantity: Float!
  $price: Float!
  $productVariantId: Int!
) {
  addToCart(
    sku: $sku
    quantity: $quantity
    price: $price
    productVariantId: $productVariantId
  ) {
    sku
    quantity
    price
    productVariantId
  }
}
