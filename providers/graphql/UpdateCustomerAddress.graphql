mutation UpdateCustomerAddress(
  $customerId: String!
  $name: String!
  $address: AddressInput!
) {
  updateCustomerAddress(
    customerId: $customerId
    name: $name
    address: $address
  ) {
    ...CustomerFragment
  }
}
